# This is the task management application using python OOP and MySQL
# Make the venv first
```python
python -m venv venv
```
# Install the requirements from the requirements.txt
```python
pip install -r requirements.txt
```